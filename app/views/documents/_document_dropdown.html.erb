

<div>
	<%= form_for [@job.user, @job], :url => user_job_path(@job.user, @job), :html => {:id => "add-document", :method => :post} do |f| %>

		<p><%= f.collection_select :document_ids, @job.user.documents, :id, :title, {:include_blank => 'choose document'} %></p>	

		<%= f.submit "Link Document", {id: 'btn-add-document'} %>

	<% end %>
</div>